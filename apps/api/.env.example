# Hanna runtime note
# Hanna LLM 인증/제공자 선택은 이 예시 파일에서 직접 설정하지 않습니다.
# 앱 사용자는 시장 데이터, 브로커, 알림, 저장소 설정만 채우면 됩니다.
# (레거시 local/dev LLM provider env 예시는 제거했습니다.)

# FRED API (미국 거시지표)
# https://fred.stlouisfed.org
FRED_API_KEY=your-fred-api-key

# ECOS API (한국은행 경제통계시스템)
# https://ecos.bok.or.kr
ECOS_API_KEY=your-ecos-api-key

# DART API (전자공시 Open API)
# https://opendart.fss.or.kr
DART_API_KEY=

# 한국투자증권 Open API (선택)
# 실전: https://openapi.koreainvestment.com:9443
KIS_APP_KEY=
KIS_APP_SECRET=
# 계좌번호 앞 8자리
KIS_ACCOUNT_CANO=12345678
# 계좌 상품코드 뒤 2자리 (일반 위탁: 01)
KIS_ACCOUNT_ACNT_PRDT_CD=01

# ────────────────────────────────────────────────────────
# 주의: 이 프로젝트는 실거래 API를 사용하지만 내부 가상계좌로 처리함
# 실거래 서버 (현재 사용 중)
KIS_BASE_URL=https://openapi.koreainvestment.com:9443
# KIS 공식 모의투자 계좌 사용 시 아래로 교체
# KIS_BASE_URL=https://openapivts.koreainvestment.com:29443
# ────────────────────────────────────────────────────────

# 실행 모드: paper (내부 가상계좌) | live (실거래 주문 실제 집행)
# 절대 live로 바꾸기 전에 체크리스트 확인할 것 (docs/live-trading-checklist.md)
EXECUTION_MODE=paper

# 텔레그램 봇 (선택)
TELEGRAM_ENABLED=true
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
REPORT_WEB_URL=http://localhost:8081

# 이메일 발송 (선택)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
REPORT_RECIPIENT=

# 발송 방식 (telegram / email / both / none)
DELIVERY_METHOD=none

# 리포트/로그 경로
# 로컬 실행에서는 비워두면 기본값으로
# storage/reports, storage/logs 를 사용합니다.
# Docker 실행에서는 docker-compose 가 /reports, /logs 로 override 합니다.
