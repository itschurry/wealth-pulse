# Web Domain Map

- `operations`: runtime monitoring, order flow, scanner, performance
- `lab`: validation, preset editing, universe experiments
- `analysis`: reports, watchlist, research snapshots

The current transition keeps existing page files in place, but the navigation model and route ownership already follow these domains.
